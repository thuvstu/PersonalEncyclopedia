# AGENTS.md

このファイルは、このリポジトリで作業するすべてのコーディングエージェント(Claude Code等)が**セッション開始時に必ず読むべき**運用ルールです。設計の詳細は`DESIGN.md`と`docs/`配下を参照してください。ここには「どう作業するか」だけを書きます。

## プロジェクト概要

Personal Encyclopedia — Androidネイティブの個人知識管理(PKM)+学習アプリ。データはRoom(SQLite)に一元化し、PCはKtor経由の閲覧クライアント(DBを持たない)。完全な設計仕様は`docs/PersonalEncyclopedia-統合設計書-v15完全版.md`(最新版)、実装の現状解説は`DESIGN.md`、機能別の平易なガイドは`docs/guide/`を参照。

## ビルド・テストコマンド

```bash
# Androidアプリ
./gradlew assembleDebug          # デバッグビルド
./gradlew test                    # 単体テスト(JVM)
./gradlew connectedAndroidTest    # 計装テスト(実機/エミュレータ必要)
./gradlew lint                    # Lint

# PC Webクライアント(web/)
cd web && bun install
bun run dev                       # 開発サーバー
bun run build                     # tsc --noEmit && vite build
```

パッケージ管理・実行コマンドは`npm`/`npx`ではなく**`bun`を使う**(web/のみ。Android側はGradle)。

## 最重要: 作業の五原則

過去にこれを守らず、10機能同時導入でコンパイルエラーが連鎖しgit撤回に至ったことがある(`docs/報告書.md`参照)。**必ず守ること。**

1. **1セッション1機能**。複数機能を同時に導入しない。1ファイルごとに提示→ビルド確認→コミットを繰り返す。まとめて出さない
2. **既存コードが唯一の正**。クラス名・メソッド名・パラメータ名を推測で書かない。使う前に実際のファイルを読んで確認する
3. **提示前に依存を検証する**。呼び出す側を書く前に、呼び出される側(DAO・Composable関数のシグネチャ等)を実際に開いて確認する
4. **エラー3ファイル基準**。ビルドエラーが3ファイル以上に波及したら個別修正を中止し、直前の安定コミットへ戻ってから1機能ずつ再導入する
5. **動くコード > 設計網羅性**。「実装して」「全部まとめて」という指示は、設計書の全項目をコード化することではなく、今動くものを1つ増やすことだと解釈する

## セッション開始時チェックリスト

```
このセッションで着手するのは [1機能のみ] とする。
1. 対象範囲の既存コード(呼び出し元・呼び出し先)を実際に読み、
   参照するクラス・メソッド・パラメータ名を確認してから書き始める。
2. 既存コードの変更が必要な場合、変更箇所を最小限にし、変更前後の差分を明示する。
3. 1ファイルごとに提示→ビルド確認→コミット、を繰り返す。まとめて出さない。
4. ビルドエラーが3ファイル以上に波及したら、その場で個別修正をやめて
   「直前のコミットに戻して1機能ずつ再導入する」ことを提案する。
5. このセッションの完了条件は「ビルドが通り、実際にインストールして動作確認できる」こと。
```

## コミット・プッシュ・記録のルール

- **ビルド確認のタイミング**: 1つの実装をひと息に終え、依存関係のエラーがすべて解消された状態になったら、その時点でビルドが通ることを必ず確認する
- **コミット**: 毎回コミットする(1ファイル or 1機能ごと)
- **プッシュ**: 実装の各層(DB/Compose/画像/起動/検索等)で一段落ついたタイミングでまとめてプッシュする
- **walkthrough記録**: 作業のまとまりごとに`docs/walkthroughN.md`(既存の続番)を書き、何を実装したか・何を確認したかを記録する
- **ドキュメント更新**: 作業後、`DESIGN.md`と`docs/guide/`配下の該当ファイルを実装の実態に合わせて更新する。「作ったが誰も読めるドキュメントが無い」状態を作らない

## 絶対に壊してはいけない骨格

- Android = データ本体(Room DB)。PC = 閲覧専用でDBを持たない
- Google Drive API/OAuthは使わない。クラウド連携はSAF(Storage Access Framework)方式
- 接続候補(`connection_candidate`)は自動でconnectionへ書き込まない。必ず承認フローを経由する(`AUTO_CONNECT_ENABLED`は既定false)
- entry統一型+Class Table Inheritance(13型+拡張)のパターンを崩さない
- 月額固定費ゼロ・無料枠優先(Gemini free tier / Ollama / OpenRouter free tier)

## 現在のマイルストーン状況

Phase 0〜4相当(entry全13型・ホワイトボード・Wiki・FSRS・Ollama・接続承認制・コーチング・GAP-1〜6・タスク管理・StudyPlus連携・SQL Explorer)は実装済み・実機ビルド確認済み。現在はパフォーマンス改良フェーズ(`docs/PersonalEncyclopedia-パフォーマンス大改良計画.md`参照、目標: 50,000件規模での実測)。詳細な次の一手は同ファイルのRound構成に従うこと。