# Personal Encyclopedia — 全タスク一覧 v3(プロトコル統合版)

**作成日:** 2026-08-10
**土台:** v2(zako_task.txt統合版) + 報告書.md(実装困難点・撤回原因分析)の教訓を統合
**このバージョンで変えたこと:**
1. 報告書の5教訓を「作業プロトコル」として明文化し、コーディングエージェントへ毎セッション渡せる形にした
2. 報告書Part1の具体的な落とし穴を、該当タスクへ実装メモとして注記した
3. 「推奨着手順」を、新テーブル・新DAOメソッドを一切増やさないタスクから順に並べる形へ全面組み替えた
4. nextPKM.txt(PWA/SQLite-WASM案)を検討した上で不採用と記録した

---

## 0. 前回までの経緯(要約)

v11.0設計統合 → v12.0で10機能同時導入を試みる → コンパイルエラーの組み合わせ爆発 → git撤回 → 撤回原因の自己分析(報告書.md) → 今ここ。

**被害統計(記録として残す。次回の目安値にする)**

| 項目 | 数値 |
|---|---|
| 提示した新規・変更ファイル総数 | 約45 |
| ビルドエラーを引き起こしたファイル | 7以上 |
| エラーの種類(重複除外) | 約12種 |
| 同時に導入しようとした機能数 | 10 |
| ユーザーが「わからない」「助けて」と言った回数 | 2回 |
| git撤回の回数 | 1回 |

---

## 1. 作業プロトコル(報告書の教訓を運用ルール化・最重要)

### 1.1 五原則

| # | 原則 | 内容 |
|---|---|---|
| 1 | **1セッション1機能** | 複数機能を同時に導入しない。1つ実装したらビルドを通してから次へ。1機能内の複数ファイルも、まとめて提示せず1つずつコミット+ビルド確認する |
| 2 | **知識庫(既存コード)が唯一の正** | 既存クラス・メソッド・パラメータ名を推測で書かない。新規追加は既存コードを1行も変えずに実現できないかをまず考える |
| 3 | **提示前に依存を検証する** | ファイルAを提示する前に、Aが呼ぶ全クラスのpublic API(メソッド名・パラメータ名・型・個数)を既存コードで確認してから書く |
| 4 | **エラー3ファイル基準** | 個別修正で追える上限は2ファイルまで。3ファイル以上にエラーが波及したら個別修正を即中止し、直前の安定コミットへ戻ってから1機能ずつ再導入する |
| 5 | **動くコード > 設計網羅性** | 「実装して」「全部まとめて」という指示は、設計書の全項目をコード化することではなく、今動くものを1つ増やすことだと解釈する |

### 1.2 セッション開始時チェックリスト(コーディングエージェントに毎回渡す)

```
このセッションで着手するのは [1機能のみ] とする。
1. まず対象範囲の既存コード(呼び出し元・呼び出し先)を実際に読み、
   参照するクラス・メソッド・パラメータ名を確認してから書き始める。
2. 新規追加が既存コードの変更を必要とする場合、変更箇所を最小限にし、
   変更前後の差分を明示する。
3. 1ファイルごとに提示→ビルド確認→コミット、を繰り返す。まとめて出さない。
4. ビルドエラーが3ファイル以上に波及したら、その場で個別修正をやめて
   「直前のコミットに戻して1機能ずつ再導入する」ことを提案する。
5. このセッションの完了条件は「ビルドが通り、実際にインストールして
   動作確認できる」こと。設計書の網羅ではない。
```

### 1.3 エラー対応プロトコル

```
エラー1〜2ファイル → その場で個別修正してよい
エラー3ファイル以上 → 個別修正を中止
                    → git checkout / git reset で直前の安定コミットへ戻す
                    → 原因になった機能を1段階小さく分解し直す
                    → 再度1機能ずつ導入する
```

---

## 2. 検討したが不採用とした案: nextPKM.txt(PWA / SQLite-WASM + OPFS)

React+TypeScript製のPWA。SQLite WASM(Web Worker上)+OPFSでサーバーレスにローカル永続化し、FTS5・Service Worker・Cache Storageでオフライン動作する構成。「Webアプリなのに Webサービスではない」という設計思想自体は現行案の「PC=閲覧専用・Androidがデータ本体」という発想と根は近い。

**技術的には評価できる点**
- サーバー/クラウドDBが完全不要。エコの3軸(金銭コスト)には現行案以上に強い
- OPFS+SQLite WASM+FTS5は近年のブラウザで十分成熟した組み合わせ
- OS非依存(iPhoneでも動く)。現行案のAndroid限定という制約を解消しうる

**今は不採用とする理由**
- 45ファイル以上あるKotlin/Room/Ktor/Rhinoの資産が総入れ替えになる。これは「1機能追加」ではなくアーキテクチャ全体の転換であり、原則1(1セッション1機能)の対極
- iOS SafariのOPFSはストレージ立ち退きポリシーが厳しく、「一生使うデータ基盤」の永続性保証が現状のRoom(端末ネイティブファイルシステム)より弱くなるリスク
- Android共有メニュー連携(入力摩擦ゼロの最重要機能として設計書で明記)がPWAでは実現困難、またはWeb Share Target APIでの限定対応止まり
- Rhinoプラグイン・OCR・TTS等のネイティブAPI連携が失われるか作り直しになる
- 何より、撤回直後にこの規模の転換を検討すること自体が、報告書が診断した失敗パターンの最大版になる

**部分的に活かせる箇所**: P8(PC連携)で計画済みのReact Webクライアント(I-1)に、Service Worker + Cache Storageによるオフラインシェル(UIアセットのキャッシュのみ)を追加することは検討に値する。ただし**PC側にSQLite-WASMで独立したデータストアを持たせるのは不可**とする。それをやると「Android=データ本体」の設計原則が崩れ、PCとAndroidの2つの真実のソースを同期する問題が復活する(これはSupabase案・知識庫WSL2案を不採用にした根本理由と同じ)。あくまでUIシェルのオフラインキャッシュに留める。

**再検討する条件**: 現行Android版がフェーズ3〜4まで安定して動き、実際に日常使いできる状態になった後、かつ「iPhoneでもどうしても使いたい」等の具体的な必要性が生まれた場合にのみ、独立した新規プロジェクトとして再検討する。

---

## 3. 現行ベースライン(参照用・変更なし)

| 領域 | 実装済み内容 |
|---|---|
| データモデル | entry統一型+13拡張型(CTI) / tag / topic / DB v5 |
| 学習 | SM-2 SRS / 多段階採点 / 意味的採点 / ルールベース+LLMクイズ / コーチング |
| 検索 | FTS4+Nグラム / Gemini Embedding / Hybrid Search(RRF) / LIKE |
| 接続 | connection候補承認制 / 手動接続 / Trie自動リンク / タグ表記揺れサジェスト |
| 連携 | KtorローカルAPI / Webスクレイパー(2段階) / CSV・MD・JSON・URL・Obsidianインポート |
| 永続化 | AES-256-GCMバックアップ(ローカル) / 可搬エクスポート(MD/CSV/JSON) |
| UI | 15画面 / 型別表示 / ダークモード / 添付画像 / 共有メニュー連携 |
| プラグイン | Rhinoエンジン / builtin-mcq |

v12.0で追加しようとした機能(ホワイトボード、Wiki、FSRS、Ollama、リッチテキスト等)は**すべて未反映**。以下のタスク一覧は、原則1〜5を厳格適用しながらこれらを再導入する計画。

---

## 4. タスク一覧(報告書の落とし穴を実装メモとして注記)

### 🔴 P0: データ保護・安定化

| ID | タスク | 概要 | 実装メモ |
|---|---|---|---|
| A-1 | SAFバックアップ | ユーザー指定のクラウド同期フォルダへ暗号化ファイルを出力 | 既存BackupWorkerへの追加のみ。新テーブル不要 |
| A-2 | APIキー暗号化 | EncryptedSharedPreferences/Keystoreへ移行 | 既存クラスの内部実装差し替えのみ。呼び出し側のシグネチャは変えない |
| A-3 | マイグレーションテスト | v1→v5を`MigrationTestHelper`で保証 | 既存コードは一切変更せず、テストコードのみ追加。ゼロリスク |
| A-4 | 段階的初期化+失敗隔離 | onCreateをPhase A/B/C+`runStep`に分割 | ⚠️ `runStep`はsuspend関数。`onCreate()`から直接呼ばず`appScope.launch { }`の中で呼ぶこと(過去にここで失敗した実例あり) |
| A-5 | 重複排除 | URL/メモ/クイズ/CSV/Obsidianの5経路 | 既存リポジトリへのチェック追加のみ |
| A-6 | 起動高速化 | INDEX_BUILTフラグ+ベクトルインデックス遅延ロード | A-4完了後 |
| A-7 | N+1解消 | 一括取得クエリの追加 | 既存DAOへのメソッド追加のみ |
| A-8 | 和暦マスタ整備 | 「1600年=慶長5年」が採点できないバグの修正 | 元号は248個あるが全網羅は不要。江戸期以降+著名な古典元号(天正・慶長・元禄等、歴史教育で頻出するもの)を優先し、ハードコードではなくJSON/テーブルデータとして持たせ、後から件数を増やせる形にする |
| A-9 | バックアップ成功可視化 | 最終成功日時の表示、失敗継続時の通知 | A-1とセットで実施 |
| A-10 | LAN通信リスクの警告 | 「信頼できるWi-Fiでのみ有効化」の警告文追加 | 設定画面へのテキスト追加のみ |
| A-11 | スレッドセーフ化 | `InMemoryVectorIndex`の並行アクセス対応 | Mutexではなくimmutable配列のAtomicReference差し替えを推奨(読み取りが多いワークロードのため) |

### 🟠 P0.5: テスト基盤ゲート(ここが閉じるまでP1以降のスキーマ変更に着手しない)

| ID | タスク | 概要 |
|---|---|---|
| J-1 | 採点エンジンテスト | 「1600年=慶長5年」を明示テストケースに含め、A-8修正のred→greenの起点にする |
| J-2 | SRSテスト | Sm2Algorithmのinterval/ease検証 |
| J-3 | 検索テスト | NgramTokenizer/cosineSimilarity/RRFマージ |
| J-4 | 並行アクセステスト | A-11の検証 |

### 🟡 P1: データベース拡張

> ⚠️ **entry統一型+CTIへの変更共通の注意**: 1つの型に触ると `EntryRepository.create*()` / `EntryEditScreen`のwhen分岐 / `EntryEditViewModel` / `EntryTypeSections` / `EmbeddingTextBuilder` の最低5箇所が連鎖して影響する。新型・新フィールド追加時は、この5箇所すべてへの影響を最初に洗い出してから着手する

| ID | タスク | 概要 | 実装メモ |
|---|---|---|---|
| B-1 | ホワイトボード系テーブル | whiteboard等4テーブル(v6マイグレーション) | ⚠️ `entities`配列への追加・`version`変更・`DatabaseModule`の`@Provides`追加の3点セットが必要。1つでも欠けるとRoomのコンパイル時検証(KSP)が失敗する。3ファイルを同一コミットでまとめて揃える |
| B-2 | Wikipedia記事テーブル | wiki_article(v7マイグレーション) | B-1と同じ3点セットの注意が適用される |
| B-3 | カスタムフィールドテーブル | entry_custom_field | 最小スコープ(1テーブルのみ)とし、既存13型のUIには一切触れない設計にする |

### 🟡 P2: 学習エンジン強化

| ID | タスク | 概要 | 実装メモ |
|---|---|---|---|
| C-1 | FSRS移行 | SRS_ALGORITHM設定でSM-2/FSRS切替 | `SrsRepository.recordReview`の分岐のみで実現し、既存SM-2コードは変更しない(残す)。移行前に`repetitionCount`カラムを追加しておく |
| C-2 | クイズ一覧画面 | 一覧・フィルタ・ソート・攻略度表示 | 既存`QuizDao`/`QuizBankEntity`のみで完結。新テーブル不要 |
| C-3 | 数値可変問題 | numericVariantConfigJsonのパース+再抽選 | — |
| C-4 | クイズ重複排除 | 生成時のquestion重複チェック | ⚠️ 存在しないDAOメソッド(`countByQuestion`等)を書く前に、`QuizDao`の実メソッド一覧を確認する |
| C-5 | コーチング強化 | analyzeWeakPointsのtopicId反映 | — |

### 🟡 P3: AI・LLM強化

| ID | タスク | 概要 |
|---|---|---|
| D-1 | Ollama対応 | OllamaClient実装、provider切替 |
| D-2 | Geminiモデルレジストリ | AiModelsでモデル+ツール対応を管理 |
| D-3 | 2段階ファクトチェック | grounding調査→構造化生成 |
| D-4 | 教科別AI執筆ルール | prompts/への分離 |
| D-5 | AI設定UI | Settings画面への追加 |

### 🟢 P4: 検索・接続強化

| ID | タスク | 概要 | 実装メモ |
|---|---|---|---|
| E-1 | 検索ソート/フィルタ強化 | ソート・型・タグ・日付フィルタ | — |
| E-2 | タグタップ検索 | タグチップから検索画面への遷移 | NavGraphへのクエリパラメータ追加のみ。新テーブル・新DAO不要 |
| E-3 | リサーフェシングUI強化 | 再浮上・整理候補+ミュート導線 | — |
| E-4 (新) | 接続承認フローの二重チェック整理 | `generateCandidatesForEntry`の`autoConnectEnabled`判定をEngine層に統一し、承認時`createManualConnection`との重複チェックを一本化する | 現状ViewModel層とEngine層のどちらで判定するか揺れており、承認フローで二重チェックが発生している |

### 🔵 P5: UI・UX新機能

| ID | タスク | 概要 | 実装メモ |
|---|---|---|---|
| F-1 | ホワイトボード画面 | パン/ズーム/ドラッグ/セクション | ⚠️ `detectTransformGestures`と`detectDragGestures`がイベントを奪い合う既知の問題。ジェスチャ処理は単一のpointerInputに統一するか、明確な優先順位を設計してから実装する |
| F-2 | Wikipediaビルダー | 記事一覧・閲覧・編集 | F-3完了後 |
| F-3 | リッチテキスト描画 | RichContentView(WebView+marked+KaTeX等) | ⚠️ **既存の`MarkdownText`を置き換えない**。新規コンポーネントとして追加し、既存呼び出し箇所のパラメータ名(`onWikiLinkClick`等)を独自に作り変えない |
| F-4 | カスタムフィールドUI | 自由記述項目の追加・編集 | — |
| F-5 | 遷移アニメーション高速化 | NavHost遷移を120msフェードに短縮 | NavHostの引数追加のみ |
| F-6 | 定義・解説のリンク化 | リッチ描画+自動リンク | F-3完了後 |

### 🟣 P6: インポート/エクスポート

| ID | タスク | 概要 |
|---|---|---|
| G-1 | PDFインポート | PdfBox-Android |
| G-2 | DOCX/XLSX/PPTXインポート | zip+XMLをJsoupで軽量パース |
| G-3 | Notionインポート | 既存WikiLinkParser共用 |
| G-4 | YouTube Likedインポート | YouTube Data API |
| G-5 | Drive imports連携 | WorkManagerでの定期取り込み |

### 🟤 P7: プラグイン・拡張

| ID | タスク | 概要 |
|---|---|---|
| H-1 | PluginUISchemaレンダラー | JSONをComposeで再帰描画 |
| H-2 | ゲーミフィケーション | 読み上げ/早押し/タイムアタック等 |

### ⚪ P8: PC連携(Phase 4相当・後段)

| ID | タスク | 概要 | 実装メモ |
|---|---|---|---|
| I-1 | React Webアプリ | Ktor APIを叩く入力・閲覧クライアント(DBなし) | nextPKM.txtのアイデアのうち「Service Worker+Cache StorageによるUIシェルのオフラインキャッシュ」はここに部分的に取り込める。**独立したSQLite-WASMデータストアは持たせない**(2つの真実のソースを作らないため) |
| I-2 | React Flowグラフ | ナレッジグラフ本格可視化 | — |
| I-3 | AUTO_CONNECT有効化 | 3条件を満たしたら自動化 | — |

### ⚪ P9: 品質・リファクタ

| ID | タスク | 概要 | 実装メモ |
|---|---|---|---|
| K-1 | typeの定数化 | `EntryTypes`オブジェクト導入 | — |
| K-2 | 大規模画面の分割 | `EntryEditViewModel`(800行超)/`DashboardScreen`/`EntryDetailScreen`(300〜800行)を型別・責務別に分割 | `remember`/`collectAsState`が20個を超えるとリコンポジション範囲が広がる。分割時は状態の粒度も見直す |
| K-3 | FTS外部コンテンツテーブル化 | `@Fts4(contentEntity=...)`化 | ⚠️ 現状`search_document_fts`と`search_document`をrowidでJOINしており、FTS4仮想テーブルのrowidが外部コンテンツテーブルと一致しない可能性がある。また`NgramTokenizer.buildFtsQuery()`はユーザー入力の`"`や`*`をエスケープしないとFTS4構文エラーになる。`EmbeddingQueue.updateSearchDocument()`の削除→挿入処理と`search_document`のUPSERTが競合する経路も要確認 |
| K-4 | マジックストリング排除 | `"__UNLEARNED__"`等の定数化 | — |
| K-5 (新) | Ktorルーティングの分割 | `LocalServer`が8DAOを直接注入され全エンドポイントを1クラスに集約している状態を、エンドポイント単位のファイルに分割する | `embeddedServer(Netty, port)`の返り値`EmbeddedServer<*, *>`はKotlinの型推論と衝突しやすい。DTOが20個以上あり`@Serializable`の付け忘れが1つでも全体ビルドを止める点にも注意 |

### ⚫ P-Ops: 運用ガードレール

| ID | タスク | 概要 |
|---|---|---|
| L-1 | フェーズ完了の定義固定 | 「ビルドが通る」「該当テストが通る」「3日以上使った」の3条件(既存) |
| L-2 | 自己レビュー非鵜呑みルール | 実装と同セッションのレビューは次セッションで実在確認する(既存) |
| L-3 | 無料枠フォールバック方針 | Gemini無料枠縮小時はOllamaへ切替(既存) |
| L-4 (新) | 作業プロトコル(§1)をコーディングエージェントへ毎セッション渡す | 五原則+セッション開始チェックリストをコピペで貼り付けてから作業を始める運用にする |
| L-5 (新) | エラー3ファイル基準の厳守 | 3ファイル以上にエラーが波及した時点で、個別修正を続けず即座に安定コミットへ戻す。「まとめてやって」という指示が出ても、この基準は緩めない |

---

## 5. 推奨着手順(全面組み替え: 新テーブル・新DAOメソッドを増やさない順)

各Roundの中でも、実際には1ファイルごとにコミット+ビルド確認する(原則1参照)。ここでの「Round」はまとめて提示してよい単位ではなく、リスクの低さで束ねた目安。

**Round 1 — 新テーブル・新DAOメソッドを一切増やさない(最速・最安全)**
1. A-2 APIキー暗号化
2. E-2 タグタップ検索
3. F-5 遷移高速化
4. A-7 N+1解消
5. A-5 重複排除
6. C-2 クイズ一覧
🛑 ビルド確認 → 数日使用

**Round 2 — データ保護(新テーブルなし)**
7. A-1 + A-9 SAFバックアップ+可視化
8. A-10 LAN警告文
9. A-3 マイグレーションテスト(既存v1→v5の検証のみ、まだv6は作らない)
🛑 ビルド確認 → バックアップが実際に成功し続けることを数日確認

**Round 3 — テスト基盤ゲート**
10. J-1 採点エンジンテスト → A-8 和暦マスタ修正
11. J-2 SRSテスト、J-3 検索テスト
12. A-11 スレッドセーフ化 → J-4検証
🛑 ビルド確認。ここでP0.5クリア

**Round 4 — 既存構造内での機能拡張(新テーブルなし)**
13. C-1 FSRS(分岐のみ、SM-2は残す)
14. F-3 リッチテキスト(既存MarkdownTextは置き換えない)
🛑 ビルド確認

**Round 5 — 初めての新テーブル導入(1つだけ)**
15. B-3 カスタムフィールド(最小スコープ)
🛑 ビルド確認 → 数日使用

**Round 6以降 — 大型機能(それぞれ単独セッション、教訓を厳格適用)**
16. B-1 → F-1 ホワイトボード
17. B-2 → F-2 Wikipediaビルダー(F-3完了が前提)

---

## 6. 依存関係グラフ(v2から変更なし・参照用)

```
A-3(マイグレーションテスト) ─┬→ B-1(ホワイトボード) → F-1
                            ├→ B-2(wiki) → F-2
                            └→ B-3(カスタムフィールド) → F-4
A-1 → A-9 / G-5
A-4 → A-6
A-5 → C-4
A-8 ← J-1 / A-11 ← J-3,J-4
J-2 → C-1
D-1 → D-2 → D-3 / D-4 / D-5 → G-4
F-3 → F-2 / F-6
E-3 → I-3
```

---

# v4追補(2026-08-11・設計書v14.0との同期)

設計書v14.0で具体化した内容に対応するタスクを追加する。既存のP0〜P9・Round構成は変更しない。

## 追加タスク一覧

| ID | タスク | 概要 | 対応する設計書節 | Round目安 |
|---|---|---|---|---|
| M-1 | ライブラリバージョンの再調査・固定 | 実装セッション開始時にKotlin/Compose/Room/Ktor/React/Vite/Ollamaの最新安定版を再確認し`build.gradle.kts`/`package.json`へ反映 | §4.1.1 | Round 1(継続タスク) |
| M-2 | Geminiモデルレジストリ実装 | `AiModels`クラス+`AiTask` enumを実装し、タスク別モデル割当を設定で上書き可能にする | §7.4.4 | Round 6(Ollama対応と同時) |
| M-3 | PC(React)からのOllama直接呼び出し | `ollamaClient.ts`実装、設定画面でのLANアドレス共有 | §7.7 | Round 6 |
| M-4 | クイズバリエーション: プレッシャーテスト/サバイバル形式 | 既存出題エンジンのロジック変更のみ、新規UI不要のため優先実装 | §8.7.2 | Round 6(ゲーミフィケーション拡張の最初) |
| M-5 | クイズバリエーション: 書きかけ漢字パズル/マッチング問題 | 新規UIコンポーネントが必要なため後続 | §8.7.2, §8.7.3 | Round 6以降 |
| M-6 | 自動リンクの定義プレビューポップアップ | `EntryPreviewPopup`実装 | §12.5 | Round 4〜5(リッチテキストと合わせて) |
| M-7 | 設定画面の実装 | §11.10の全項目(バックアップ・セキュリティ・AIプロバイダ・PC連携・学習設定・データ管理) | §11.10 | Round 2〜6にまたがって段階実装 |
| M-8 (最重要) | 開発者ガイドの継続執筆 | §14.1の`docs/guide/`をRoundごとに1〜2段落追記。コード・テストと並ぶ第三の成果物として扱う | §14.1 | 全Round共通・継続タスク |

## Round運用ルールの追記

§1運用ルールに以下を追加する。

- **ルールF**: 各Roundの完了条件に「該当する`docs/guide/`ファイルへの追記」を含める。コード+テストが揃ってもガイド更新がなければそのRoundは未完了とする
- **ルールG**: 新しいクイズバリエーション(§8.7)は既存プラグイン契約に従い、コア出題フロー(§8.1〜8.4)のコードには触れずに追加する。1形式=1プラグイン=1セッションとする
